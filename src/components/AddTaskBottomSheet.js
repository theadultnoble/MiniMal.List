import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AddTaskBottomSheet = () => {
  return (
    <View>
      <Text>AddTaskBottomSheet</Text>
    </View>
  );
};

export default AddTaskBottomSheet;

const styles = StyleSheet.create({});
